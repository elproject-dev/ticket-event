import { buatAcara } from './app/admin/acara/tambah/actions.js';

// Since Next.js server actions are hard to test via plain node without next server context, 
// I will just use fetch to simulate a multipart/form-data request if there was an API.
// But it's a server action.
